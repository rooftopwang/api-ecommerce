{
    "dev": {
      "driver": {"ENV": "POSTGRES_DRIVER"},
      "host": {"ENV": "POSTGRES_HOST"},
      "database": {"ENV": "POSTGRES_DB"},
      "user": {"ENV": "POSTGRES_USER"},
      "password": {"ENV": "POSTGRES_PASSWORD"}
    },
    "test": {
      "driver": {"ENV": "POSTGRES_DRIVER"},
      "host": {"ENV": "POSTGRES_HOST"},
      "database": {"ENV": "POSTGRES_DB_TEST"},
      "user": {"ENV": "POSTGRES_USER_TEST"},
      "password": {"ENV": "POSTGRES_PASSWORD"}
    }
}